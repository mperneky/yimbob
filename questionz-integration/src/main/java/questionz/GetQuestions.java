package questionz;

/**
 * Created by Yimbo on 2017.07.30..
 */
public class GetQuestions {
  // depend on service -> httpoperations

  // get the response from the endpoint
  // transform it to Question -s so the /hello endpoint can display them
  public Question[] getQuestions(String requestor) {
    return null;
  }
}
